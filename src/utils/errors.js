const PASS_VALID_PAYLOAD = 'Please pass valid payload';
const USER_NOT_FOUND = 'User not found';
const INVALID_PASSWORD = 'Invalid Password';
const INVALID_TOKEN = 'Invalid token';
const UNATHORIZED = 'Un Authorized';
const INTERNAL_SERVER_ERROR_CODE = 500;
const BAD_REQUEST = 400;
const ERROR_CODES = {
  INTERNAL_SERVER_ERROR_CODE,
  BAD_REQUEST,
};

export const ERRORS = {
  PASS_VALID_PAYLOAD,
  ERROR_CODES,
  USER_NOT_FOUND,
  INVALID_PASSWORD,
  INVALID_TOKEN,
  UNATHORIZED,
};
